// Remover esses comentários quando criar o comando.

/* eslint-disable no-unused-vars */
/* eslint-disable no-empty-function */

module.exports = {
  name: '',
  aliases: [''],
  description: '',
  cooldown: 0,
  execute(message, profileData, args) {
  },
};